# Lesada's Eslint Config

## Setup

Install dependencies: `npm i eslint-config-lesada` or `yarn add eslint-config-lesada`

Inside your eslint add:
```
{
  "extends": [
    eslint-config-lesada
  ]
}
```

Enjoy <3
